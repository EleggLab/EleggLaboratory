# 2026-03-28

- Saved the art/UI direction from `https://zb-web2.pages.dev/` as a mood reference only.
- Key reference takeaways:
  - dark zombie-survivor dossier mood
  - black/zinc base palette with blood-red accents
  - oversized hero typography and poster-like character presentation
  - map/list dual browsing and right-side profile sheet feel
  - use for tone and hierarchy, not gameplay structure

- Clarified `game_DEV/text-choice-frame-studio` scope around `pc-choice-ugc`.
- Treated `pc-choice-ugc` as the `60sec Choice Game UGC` baseline rather than spinning `ugc-python-steam` into a separate product line.

- `game_DEV/mobile-saju-fortune-toss/apps/toss` history round completed.
- Added shared `/history` flow for:
  - saju
  - tarot
  - today fortune
  - iching
- Key files added/updated in Toss app:
  - `src/legacy/lib/features/history/storage.ts`
  - `src/legacy/lib/features/history/types.ts`
  - `src/legacy/lib/features/iching/fortune.ts`
  - `src/legacy/lib/features/saju/savedInput.ts`
  - `src/legacy/lib/features/tarot/helpers.ts`
  - `src/legacy/lib/features/today/fortune.ts`
  - `src/legacy/lib/ui/HistoryLinkChip.tsx`
  - `pages/history.tsx`
- Validation passed earlier with temporary release env:
  - `corepack pnpm --filter @saju/toss typecheck`
  - `corepack pnpm --filter @saju/toss audit:parity`
  - `corepack pnpm --filter @saju/toss build`

- Toss miniapp dev environment recovery also progressed.
- `apps/toss/bedrock.config.cjs` was corrected to the proper Bedrock config path so dev server boot works again.
- Official Toss sandbox APK was installed on the emulator, but Android testing is still much easier through the Expo mobile mirror than through Toss sandbox login.

- Android Studio mirror debugging for `mobile-saju-fortune-toss` made a major breakthrough.
- Testing baseline:
  - use `C:\sm` short-path mirror workspace
  - open `C:\sm\apps\mobile\android` in Android Studio
  - use a 4 KB page-size AVD such as `Medium_Phone_API_36`
  - do not use `Pixel_9_Pro` / `ps16k` emulator for this project

- Important Android findings:
  - Hermes must stay enabled for this app on Android.
  - The old blank/black screen was not a simple Metro issue.
  - A pure RN root proved the native bridge was healthy.
  - The real blocker was the preview bootstrap path:
    - `expo registerRootComponent` path was breaking in the mirror setup
    - `preview/App.tsx` eagerly imported navigation and all screens even in simple preview mode
    - that eager import path caused early module load failures and made `main` appear unregistered

- Fixes that made the mirror actually render:
  - regenerated `C:\sm\apps\mobile\android` with `npx expo prebuild --platform android --clean --no-install`
  - switched `C:\sm\apps\mobile\index.js` to direct `AppRegistry.registerComponent('main', ...)`
  - refactored `C:\sm\apps\mobile\preview\App.tsx` to lazy-load navigation/screens only when the full preview shell is needed
  - moved `previewTheme` inside the non-simple branch so simple preview no longer references `DefaultTheme` too early

- Current working mirror state:
  - `C:\sm\apps\mobile\index.js` uses direct `AppRegistry`
  - `C:\sm\apps\mobile\preview\App.tsx` uses lazy requires
  - `SIMPLE_PREVIEW=false` and the full preview shell now launches successfully
  - release APK installs and runs on emulator without the earlier AppRegistry crash

- Smoke-tested tab navigation on the working mirror:
  - today
  - tarot
  - home
  - saju
  - iching
- No fatal crash during tab switching.
- Captures and dumps saved under `tmp/android-mobile-test`, including:
  - `mobile-appregistry-preview-lazy2.png`
  - `mobile-full-preview-check.png`
  - `tab-today.png`
  - `tab-tarot.png`
  - `tab-home.png`
  - `tab-saju.png`
  - `tab-iching.png`
  - matching `*.xml` UI dumps

- Current follow-up direction:
  - keep using `apps/mobile` mirror for Android Studio QA
  - step through deeper flows from the now-working preview shell
  - compare mirror UX against original mobile screens and keep parity as high as possible

- Android mobile follow-up fixes for the fortune app:
  - Reproduced the tarot failure more precisely: the crash was not on the tarot tab itself, but when completing a tarot draw and entering the result screen.
  - Log capture for the reproduced bug:
    - `tmp/android-mobile-test/logcat-tarot-after-complete.txt`
    - runtime error was `TypeError: undefined is not a function` in the tarot result transition
  - Hardened `apps/mobile/app/(tabs)/tarot/result.tsx` in both the mirror `C:\sm` copy and the main repo copy.
    - wrapped tarot result derivation in defensive `try/catch`
    - guarded tarot image resolution
    - added a text fallback card body when image resolution fails
  - Confirmed the previously reproduced tarot result crash no longer throws after the fix:
    - `tmp/android-mobile-test/logcat-tarot-fixed.txt`

- Home character interaction polish:
  - Renamed the home character voice to `아스트라`
  - Added multiple Astra lines including `저는 아스트라라고 해요. 오늘의 흐름을 함께 읽어드릴게요.`
  - Made the entire home hero area tappable so any tap on the home screen changes the line, not just the bubble itself
  - Verified line rotation via UI dumps:
    - `tmp/android-mobile-test/home-astra-before.xml`
    - `tmp/android-mobile-test/home-astra-after-tap.xml`

- App icon update:
  - Replaced `app-icon.png`, `adaptive-foreground.png`, and `splash-icon.png` in both the mirror app and the main repo app.
  - Used `C:\Users\rndhr\Downloads\DSAD.png` as the best local match for the icon image the user attached in chat.
  - Backed up previous assets with `.backup-20260328`.

- Android mirror stability follow-up:
  - Reproduced the remaining tarot blank-screen issue inside the Android Studio mirror and confirmed it was a mirror-only route parser failure, not a tarot content bug.
  - Root cause: `C:\sm\apps\mobile\preview\router.tsx` used `URLSearchParams(...).entries()` on Hermes during tarot route transitions, which threw `TypeError: undefined is not a function`.
  - Fixed the mirror preview router by replacing the query parsing with a manual decoder that does not rely on `URLSearchParams.entries()`.
  - Also optimized tarot card tiles in both the mirror and the main repo so front-face tarot images only render after a card is flipped face-up:
    - `C:\sm\apps\mobile\app\(tabs)\tarot\TarotCardTile.tsx`
    - `game_DEV/mobile-saju-fortune-toss/apps/mobile/app/(tabs)/tarot/TarotCardTile.tsx`
  - Fixed the oversized tiger icon mapping in both the mirror and the main repo daily fortune screen by switching to the lighter zodiac-specific tiger asset:
    - `C:\sm\apps\mobile\app\(tabs)\today.tsx`
    - `game_DEV/mobile-saju-fortune-toss/apps/mobile/app/(tabs)/today.tsx`
  - Release APK was rebuilt from `C:\sm\apps\mobile\android` and manually reinstalled with `adb install -r`.
  - Emulator frame drops and UI oddities were confirmed to be heavily mirror/emulator-side:
    - saw skipped-frame warnings in logcat
    - hit repeated `System UI isn't responding` popups until the AVD was rebooted
    - after rebooting `Medium_Phone_API_36`, screenshot capture and navigation became reliable again

- Curated Android screenshot set:
  - Captured and curated 20 valid high-resolution screenshots under:
    - `tmp/android-mobile-test/shots-20260328-final20`
  - Final set includes:
    - home/Astra dialogue variants
    - daily fortune list + western zodiac detail
    - 12-animal section + rat/tiger/dog detail screens
    - tarot home + today result + love reading + love result
    - saju form + result + scrolled lower result
    - iching intro + result

- Market screenshot refresh:
  - Removed the red `PREVIEW RUNNING` badge from the Android Studio mirror preview shell and rebuilt the release APK from `C:\sm\apps\mobile\android`.
  - Re-captured the requested 7 market screens without the preview badge and saved the full-height originals to:
    - `tmp/android-mobile-test/market-7-raw`
  - Exported a portrait-trimmed store-ready set to:
    - `tmp/android-mobile-test/market-7-final`
  - Used the Astra self-introduction line for the home shot and rebuilt the daily `쥐띠` detail image with a clean header strip so the rat detail keeps the correct content while removing the badge.
