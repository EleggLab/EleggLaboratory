# 2026-04-18

- Workspace clone consolidation
  - Preserved the active working repo at `Documents\GitHub\EleggLaboratory` as the canonical copy inside the writable workspace.
  - Imported `toss-game-rebuilds/` from the clean clone at `Documents\GitHub\aaa\EleggLaboratory`.
  - Preserved OneDrive-only artifacts by copying `nibel_best_sets/` into the current workspace and recovery notes/scripts into `data/recovery/onedrive-2026-04-18/`.
  - Could not directly edit or delete the external `OneDrive` or `aaa` paths from this session due sandbox policy, so added `scripts/consolidate-elegglaboratory-clones.ps1` to sync into `aaa` and then move the OneDrive copy to the Recycle Bin when run locally.
  - Reorganized the `aaa` workspace so reusable assets and documents live under `shared-assets/` and research/reference material lives under `research/`.
  - Moved root-level shared folders into stable hubs:
    - `아스트라` -> `shared-assets/characters/astra`
    - `마법 물약` -> `shared-assets/project-docs/magical-potion`
    - `nibel_best_sets` -> `shared-assets/nibel/best-sets`
    - `참고자료` -> `research/reference-library/vibe-coding-kit`
  - Added workspace governance docs under `docs/workspace/` covering structure, naming conventions, shared resources, and the project catalog.
  - Updated Astra pipeline references so `game_DEV/mobile-saju-fortune-toss-10pass` now reads shared assets from `shared-assets/characters/astra`.
  - Cleaned repo push blockers by moving all local `.ait` build artifacts out of the repo into `C:\Users\rndhr\Documents\GitHub\aaa\_local-backups\EleggLaboratory\2026-04-18-git-prune`.
  - Backup size for preserved release bundles is `582.1 MB`, safely below the requested `2 GB` ceiling.
  - Removed generated caches and build outputs from the working tree, including `unity-mcptest/Library`, `game_DEV/mobile-saju-fortune-toss/apps/toss/.granite`, `apps/toss/dist`, `apps/toss/.bedrock`, package `dist/` folders, and temporary backup PNG files.
  - Root `.gitignore` now blocks `.ait`, `.tsbuildinfo`, and mobile-saju-fortune-toss generated folders so GitHub Desktop will stop surfacing them as commit candidates.

- `game_DEV/mobile-breaking-block-augment-toss`
  - 보스 처치 후 증강 제안을 균등 랜덤에서 상황 인식형 제안으로 변경.
  - `rollAugmentOffer`에 앵커 선택 + 가중치 선택 + 톤 다양성 편향을 추가해, 위기 구간에는 `safety_net`, 보스 준비 구간에는 `boss_crack`, 수확 크루 구간에는 `pickup_echo`가 더 자연스럽게 뜨도록 조정.
  - 스모크 테스트에 증강 제안 시나리오를 추가해 `safety_net`, `boss_crack`, `pickup_echo` 노출과 최소 2개 톤 다양성을 검증.
  - 검증 통과: `corepack pnpm run typecheck`, `corepack pnpm run test`, `corepack pnpm run build:web`.
  - 런 종료 메타 보상 구조를 추가. 새 증강 발견 +1젬, 새 보스 발견 +1젬, 새 보스 구간 최고 기록 도달 +1젬.
  - 종료 배너와 최근 결과 요약에 연구 보너스 젬 내역이 함께 표시되도록 변경.
  - 스모크 테스트에 메타 보상 지급/중복 미지급 검증을 추가.
  - 플레이 탭 UX 정리: hero 다음에 `이번 런 준비` 카드를 배치하고, 이어하기/새 런/QA 링크를 한 패널로 묶음.
  - 승무원 라인업은 준비 카드 바로 아래로 올리고, 보스 로드맵과 기록 패널은 아래로 내려 첫 사용자의 시작 흐름을 단순화.
  - 검증 통과: `corepack pnpm run typecheck`, `corepack pnpm run build:web`.
