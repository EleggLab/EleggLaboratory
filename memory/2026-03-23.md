# 2026-03-23

- Continued hardening `game_DEV/mobile-saju-fortune-toss/apps/toss` for Toss App-in-Toss RN 2.x submission.
- Corrected Toss TDS `TextField` usage in `apps/toss/src/legacy/app/(tabs)/saju/index.tsx` from `onChange` to `onChangeText`, which was a likely runtime/type mismatch against official TDS docs.
- Added `apps/toss/scripts/validate-release-env.mjs` and wired `apps/toss/package.json` build script to block release builds when console app name, icon URL, or banner ad IDs are missing/placeholder.
- Updated Toss docs and env example to clarify that `TOSS_BRAND_ICON_URL` should use the actual icon URL from Toss console app info.
- Replaced several Toss-facing CTAs with TDS buttons in `iching`, `tarot/reading`, and `tarot/result` while preserving the existing visual tone.
- Added `apps/toss/TOSS_COMPLIANCE_AUDIT_KO.md` summarizing compliance status, remaining blockers, and additional development priorities.
- Local install/typecheck/build still remain blocked in this environment because `pnpm install` cannot reach the npm registry and earlier attempts failed with fetch/EACCES-related errors.
- Switched focus to App Store packaging for `apps/mobile` after confirming `apps/toss` is only for Toss console `.ait` uploads, not Apple submission.
- Hardened `apps/mobile` for Expo EAS iOS release by updating `app.json`, expanding `apps/mobile/eas.json`, and adding `apps/mobile/scripts/validate-app-store-config.mjs`.
- Added `apps/mobile/APPLE_RELEASE_KO.md` plus root/mobile scripts for `build:ios` and `submit:ios`.
- Verified App Store config validation passes with warnings only, but actual `build:ios:store` still fails in this environment because `eas` CLI is not installed and workspace dependencies are not installed.
- Simplified the Apple release path toward a free app with no ads or in-app purchases, updated the app description accordingly, and added `apps/mobile/APP_STORE_METADATA_FREE_KO.md`.
- Added `apps/mobile/scripts/run-eas-release.mjs` so future build/submit commands fail with a clear action message instead of a vague shell error when EAS CLI is missing.
- Added draft web pages for App Store policy/support URLs at `apps/web/app/privacy/page.tsx` and `apps/web/app/support/page.tsx`, backed by `apps/web/lib/appStoreInfo.ts`.
- Added `apps/mobile/APP_REVIEW_NOTES_FREE_KO.md` for App Review notes and linked all free-app submission docs together.
