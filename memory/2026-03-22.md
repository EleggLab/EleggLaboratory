# 2026-03-22

- Created a handoff-ready copy of `game_DEV/mobile-saju-fortune` at `tmp/exports/saju-handoff-20260322-170515/mobile-saju-fortune`.
- Created transfer zip at `tmp/exports/saju-handoff-20260322-170515/mobile-saju-fortune-transfer-20260322-170515.zip`.
- Included in-project source, docs, and mobile assets/resources.
- Excluded sensitive/local-only files: `apps/web/.env.local`, Android keystores, `keystore.properties`, `local.properties`, and `apps/web/tsconfig.tsbuildinfo`.
- Added `HANDOFF_NOTES.md` inside the transfer copy for the receiving developer.
- Added `PROJECT_HANDOFF_KO.md` with app overview, structure, run commands, current QA/resource status, and prioritized remaining work for handoff.
- Cleaned the transfer copy to reduce confusing external-path artifacts by removing `apps/mobile/.asset-meta.csv` and `docs/CONFLICT_DIAGNOSIS_UGC.md`.
- Added `OPEN_THIS_FOLDER_FIRST.txt` to make it clear the recipient should open the monorepo root folder, not only `apps/mobile` or `apps/web`.
- Created a cleaner handoff zip at `tmp/exports/saju-handoff-20260322-170515/mobile-saju-fortune-transfer-clean-20260322-215151.zip`.
- Sent `BOOTSTRAP.md` to the Recycle Bin after session bootstrap instructions were completed.
- Removed the previous `tmp/exports/saju-handoff-20260322-170515` handoff bundle by sending it to the Recycle Bin.
- Created a Toss-specific working copy at `game_DEV/mobile-saju-fortune-toss` while leaving `game_DEV/mobile-saju-fortune` untouched.
- Added a new `apps/toss` Granite/App-in-Toss scaffold with `pages/_app.tsx`, `granite.config.ts`, `require.context.ts`, Toss page shells, and root scripts `dev:toss`, `build:toss`, `qa:toss`.
- Copied the mobile app's screens/assets/lib into `apps/toss/src/legacy` and rewired the tarot flow to a Toss-specific navigation context.
- Added conservative banner ad support for Toss RN using `InlineAd` with placements `home_banner_list`, `today_banner_inline`, and `tarot_result_banner_list`.
- Added Toss handoff docs at `apps/toss/README.md` and `apps/toss/TOSS_APP_HANDOFF_KO.md`.
- Verified that only excluded legacy `_layout.tsx` files still reference `expo-router`; active Toss pages use Granite routing.
- Attempted `corepack pnpm --filter @saju/toss typecheck` and `corepack pnpm install`, but both are currently blocked because `node_modules` are absent and registry access fails with `EACCES`/meta fetch errors in this environment.
- Reviewed the nested paused Toss conversion at `game_DEV/mobile-saju-fortune-toss/mobile-saju-fortune/mobile-saju-fortune` and reused its safer patterns instead of its older SDK versions.
- Updated `apps/toss/src/platform/useMiniRouteController.ts` to use Granite object-style `navigation.navigate({ name, params })`, matching the older local Toss conversion style.
- Updated `apps/toss/src/ads/TossBannerAd.tsx` to dynamically load `InlineAd` and collapse the banner slot on `no-fill` or render failure.
- Added `apps/toss/src/ads/useFullscreenAd.ts` plus fullscreen ad config placeholders so interstitial/rewarded support exists structurally but remains disabled for v1 via `TOSS_FULLSCREEN_ADS_ENABLED = false`.
- Added `apps/toss/PREVIOUS_TOSS_REFERENCE.md` so the nested older conversion remains documented as a local reference for future work.
