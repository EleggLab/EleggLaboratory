# 2026-03-28

- User asked to save only the art/UI direction from `https://zb-web2.pages.dev/` for future reference.
- Saved the reference as a dark zombie-survivor dossier style rather than a gameplay reference.
- Key art direction:
  - Black/zinc base palette with strong blood-red accents and danger-state glow.
  - Poster-like 3:4 character art used as the primary visual payload.
  - Survivor portraits treated like collectible profile cards, not tactical unit sprites.
  - 부산 urban-apocalypse mood carried by logo, ominous copy, BGM, and dark map presentation.
- Key UI direction:
  - Cold cinematic landing screen -> direct transition into a database-style main screen.
  - Very simple top navigation with only `지도` and `캐릭터`.
  - Dual browsing pattern: geographic map view plus card-grid list view.
  - Character detail shown in a right-side overlay sheet with dense profile metadata.
  - Frosted/dim panels, red active states, mono English labels, oversized hero typography, and light motion/hover polish.
- Important framing:
  - Use this reference for mood, visual hierarchy, and presentation tone only.
  - Do not treat it as a model for gameplay systems; the source reads more like a worldbuilding/character database prototype than a full game loop.
- For `mobile-saju-fortune-toss`, Android Studio testing was switched to the Expo mobile mirror rather than the Toss miniapp runtime.
- `apps/mobile` was confirmed to be the closest runtime mirror of the Toss app for fortune flows/assets; the main differences are Toss shell/TDS/runtime bridges.
- Fixed `apps/mobile/metro.config.js` by simplifying it to Expo SDK 52 default config. The old custom monorepo Metro config was contributing to Windows duplicate-scan and resolver mismatch issues.
- Direct Android debug install now works from a minimal short-path mirror workspace at `C:\sm` containing only `apps/mobile`, `packages/core`, and `packages/data`.
- Successfully built and installed `com.rndhr.sajucompanion` onto the Android emulator from `C:\sm\apps\mobile\android`.
- Metro also runs cleanly from `C:\sm\apps\mobile` on port `8081`, and the previous `isAssetFile is not a function` bundle failure disappeared in the minimal workspace.
- The emulator may show a one-time Android 16KB page size compatibility warning dialog for the debug build; it is a system warning, not an app crash.
