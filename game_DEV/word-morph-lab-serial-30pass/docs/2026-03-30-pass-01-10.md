# Word Morph Lab Pass 01-10 Log

작업 기준:

- 방식: `50-Pass Branch Critique Loop`
- 모드: 엄격한 순차 direct-edit handoff
- 범위: `Pass 01-10`
- 대상: `game_DEV/word-morph-lab`

## Trunk Summary

- 기본 루프는 `단어 입력 -> 초상 변화 -> 로그 확인 -> 해금/아카이브 수집`이다.
- 화면의 authored trunk는 `무대 중심`, `오른쪽 콘솔/보조 패널`, `하단 진행률/아카이브` 구조다.
- 이 trunk는 유지한다. 이번 라운드는 갈아엎지 않고 읽힘과 탐색성만 가지처럼 덧붙인다.

## Pass 01 - Trunk Keeper

- Preserve:
  - 무대 중심 구성
  - 콘솔 중심 입력 리듬
  - dossier-like dark mood
- Critique:
  - 세션 상태가 흩어져 있어 현재 목표가 한눈에 들어오지 않는다.
- Edit:
  - 구조를 바꾸지 않고 hero 아래에 세션 상태 strip을 추가한다.

## Pass 02 - Structure Critic

- Preserve:
  - 상단 hero와 본문 grid의 2단 진입 구조
- Critique:
  - `필터`, `힌트`, `스트릭` 같은 상태가 서로 다른 카드에 퍼져 있다.
- Edit:
  - active filter / active hint / best streak를 한 줄 strip으로 묶는다.

## Pass 03 - Hierarchy Critic

- Preserve:
  - hero copy의 큰 타이포와 stage 정보 블록
- Critique:
  - blank input 상태에서 다음 행동이 약하다.
- Edit:
  - 입력창이 비어 있을 때 starter suggestion을 바로 보여준다.

## Pass 04 - Interaction Critic

- Preserve:
  - 직접 입력 중심 상호작용
- Critique:
  - 첫 진입에서 어떤 단어부터 시도할지 막막할 수 있다.
- Edit:
  - 현재 필터에 맞춘 starter suggestion chips를 즉시 제출 가능한 액션으로 제공한다.

## Pass 05 - Mood Critic

- Preserve:
  - 차가운 패널과 초상 중심 긴장감
- Critique:
  - 세션 상태 정보가 너무 중립적이면 세계관 감각이 약해진다.
- Edit:
  - session strip도 동일한 dossier 계열 톤으로 처리하고 현재 상태 accent를 은은하게 받는다.

## Pass 06 - Craft Critic

- Preserve:
  - 기존 패널 간격과 radius 리듬
- Critique:
  - 통계는 있지만 best streak가 숨어 있어서 성취감 전달이 약하다.
- Edit:
  - best streak를 session strip에 고정 노출한다.

## Pass 07 - Accessibility Critic

- Preserve:
  - 기존 가독성 높은 dark contrast
- Critique:
  - hint가 있어도 현재 어떤 힌트를 따라가고 있는지 텍스트로 명확하지 않다.
- Edit:
  - session strip에 현재 hint target 요약을 노출한다.

## Pass 08 - Anti-Generic Critic

- Preserve:
  - 일반 dashboard처럼 보이지 않는 초상-중심 구조
- Critique:
  - 세션 상태 요약이 analytics 위젯처럼 보이면 generic drift가 생긴다.
- Edit:
  - 수치 위젯이 아니라 “현재 추적 중인 상태”를 보여주는 운영 패널 성격으로 유지한다.

## Pass 09 - Platform Critic

- Preserve:
  - mobile에서 세로 stacking되는 안전한 레이아웃
- Critique:
  - session strip이 모바일에서 한 줄로 강제되면 답답할 수 있다.
- Edit:
  - responsive grid로 만들어 모바일에선 세로 stacking되게 한다.

## Pass 10 - Final Integrator

- Preserve:
  - trunk 레이아웃, dark dossier mood, direct-input game feel
- Improve:
  - 세션 상태 가시성
  - 초기 탐색 진입
  - filter/hint 목적성
- Applied branch:
  - hero 아래 session strip 추가
  - starter suggestion flow 추가
  - 현재 필터 / 현재 힌트 / best streak 노출

## Applied Output

- files:
  - `game_DEV/word-morph-lab/index.html`
  - `game_DEV/word-morph-lab/app.js`
  - `game_DEV/word-morph-lab/styles.css`
