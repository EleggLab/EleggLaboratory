# 2026-03-29

- Added a top-level workspace policy file:
  - `PROJECT_UIUX_POLICY.md`
- Recorded the user's design authorship requirement:
  - the base UI/UX frame of the project is the user's own original work
  - future revisions should preserve that trunk and branch from it rather than overwrite it
- Set a new default art/UI workflow:
  - use the `50-Pass Branch Critique Loop` by default
  - consciousness roles repeat across 5 rounds until pass 50
- Wired that policy into:
  - `AGENTS.md` session startup
  - `AGENTS.md` project UI/UX defaults
  - `MEMORY.md` long-term preferences
- Added a top-level reusable prompt document:
  - `UIUX_10PASS_BRANCH_CRITIQUE_PROMPT.md`
- Folded in reusable ideas from existing work:
  - trunk-preservation and additive change rules from the StackCraft master design docs
  - mood-board and tone guidance from `MEMORY.md` and `memory/2026-03-28.md`
  - prompt hygiene and negative-prompt guardrails from `game_DEV/word-morph-lab/request.json`
- Created a safe experiment copy of the fortune project:
  - source: `game_DEV/mobile-saju-fortune-toss`
  - copy: `game_DEV/mobile-saju-fortune-toss-10pass`
- Applied the first concrete branch-critique refinement to the copied mobile app home screen:
  - file: `game_DEV/mobile-saju-fortune-toss-10pass/apps/mobile/app/(tabs)/home.tsx`
  - preserved trunk: full character composition + bottom dialogue identity
  - refined: Astra line quality, dialogue readability, subtle atmosphere, touch-discovery hint
- Added project-local carry-forward docs in the copied project:
  - `10PASS_COPY_NOTES.md`
  - `docs/HOME_10PASS_BRANCH_APPLIED.md`
  - `docs/UIUX_10PASS_BRANCH_CRITIQUE_PROMPT.md`
- Continued the same loop on more copied mobile screens:
  - `game_DEV/mobile-saju-fortune-toss-10pass/apps/mobile/app/(tabs)/today.tsx`
  - `game_DEV/mobile-saju-fortune-toss-10pass/apps/mobile/app/(tabs)/tarot/index.tsx`
  - `game_DEV/mobile-saju-fortune-toss-10pass/apps/mobile/app/(tabs)/tarot/result.tsx`
- Added follow-up applied notes:
  - `game_DEV/mobile-saju-fortune-toss-10pass/docs/DAILY_AND_TAROT_10PASS_APPLIED.md`
- This round focused on preserving trunk while improving:
  - reading order
  - discoverability
  - subtle atmosphere
  - anti-generic differentiation
- User reinforced the workspace startup order for future sessions:
  - read `AGENTS.md`
  - then `PROJECT_UIUX_POLICY.md`
  - then `UIUX_10PASS_BRANCH_CRITIQUE_PROMPT.md`
  - then `MEMORY.md`
  - then today's and yesterday's `memory/YYYY-MM-DD.md`
- User also explicitly locked the default for future UI/art work:
  - treat the base UI/UX as user-authored trunk
  - use the `50-Pass Branch Critique Loop` by default
- User upgraded the default critique depth from 10 to 50 passes.
- `UIUX_10PASS_BRANCH_CRITIQUE_PROMPT.md` keeps its legacy filename, but its content now defines the 50-pass workflow.
- User clarified the intended meaning of that workflow:
  - not 50 role-based evaluations first and then one later implementation pass
  - instead, each pass directly edits the incoming working branch and hands it to the next pass
  - pass 50 itself is the final revised output
- Applied another compressed 50-pass refinement round to `game_DEV/word-morph-lab`.
- Added:
  - input suggestions and near-match recovery
  - category quick-jump chips with archive filtering
  - layered hints with stronger reveal levels
  - streak tracking, category completion feedback, and archive sorting
  - keyboard shortcuts and accent-reactive background polish
- Applied one concrete 50-pass refinement run to the safe copy project:
  - target copy: `game_DEV/mobile-saju-fortune-toss-10pass`
  - target screen: `apps/mobile/app/(tabs)/iching.tsx`
  - preserved trunk: time-first divination ritual and one-tap cast flow
  - refined: hero guidance, trigram summary, 6효 visual stack, and reading order
- Updated copy-local docs for the new default:
  - `10PASS_COPY_NOTES.md`
  - `docs/UIUX_10PASS_BRANCH_CRITIQUE_PROMPT.md`
  - `docs/ICHING_50PASS_APPLIED.md`
- Validation passed in the copied project:
  - `corepack pnpm --dir game_DEV/mobile-saju-fortune-toss-10pass --filter @saju/mobile typecheck`
- Running the copied app required a local runtime workaround instead of reinstalling packages:
  - created junctions from `game_DEV/mobile-saju-fortune-toss-10pass/node_modules` and `apps/mobile/node_modules` to the source project's existing `node_modules`
  - narrowed `apps/mobile/metro.config.js` in the copy so Metro only watches shared `packages/core` and `packages/data`
  - verified Expo startup reaches `Waiting on http://localhost:8082` with:
    - `CI=1`
    - `EXPO_OFFLINE=1`
    - `EXPO_NO_DEPENDENCY_VALIDATION=1`
    - `corepack pnpm --filter @saju/mobile exec expo start --port 8082 --host localhost --max-workers 0`
  - updated `game_DEV/mobile-saju-fortune-toss-10pass/run-mobile-8082.cmd` to use the same safe startup flags
