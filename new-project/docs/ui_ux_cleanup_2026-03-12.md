# UI/UX Cleanup Note (2026-03-12)

목표: CLI 본체를 유지하면서도, 아트 에셋과 정보 구조를 확인할 수 있는 깔끔한 UI 셸을 제공한다.

## 적용 결과

- 신규 폴더: `ui/`
  - `index.html`: 대시보드 구조
  - `styles.css`: 색상/타이포/반응형 레이아웃
  - `app.js`: 저장 JSON 로더 + 라이브 상태 폴링 렌더링
  - `README.md`: 실행/사용 방법
  - `state/live_state.json`: 런타임 라이브 상태 파일(자동 생성)
- 임시 아트 에셋(`assets/temp/*.svg`)을 UI에서 실사용
  - 배경 2종, 포트레이트 2종, 아이콘 2종

## 디자인 방향

- 톤: 황무지/러스트 계열(앰버 + 차콜 + 레드 포인트)
- 구조: Hero + 6개 정보 패널(정체성/생존/세력/퀘스트/인벤토리/소문)
- UX:
  - 저장 파일 업로드 즉시 반영
  - 시연용 상태 전환 버튼(안정/붕괴 직전)
  - 배경 전환 버튼(연출 확인)
  - 라이브 동기화 토글(실행 중 CLI 상태 자동 반영)
  - 모바일 대응(1열 재배치)

## 한계 및 다음 단계

- 파일 폴링 기반 실시간 동기화는 반영됨(`ui/state/live_state.json`)
- 다음 단계:
  - WebSocket/HTTP 서버 기반 이벤트 푸시로 지연/폴링 비용 축소
  - 퍼크/퀘스트 선택 상호작용 UI 추가
  - 아트 에셋 교체 파이프라인(이름 고정 + 버전 관리) 정립
